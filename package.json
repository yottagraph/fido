{
  "name": "fido-dev",
  "private": true,
  "version": "0.0.1",
  "description": "Development workspace for the Fido fetch-project template",
  "scripts": {
    "format": "prettier --write \"**/*.{js,json,md}\" --ignore-path .gitignore",
    "format:check": "prettier --check \"**/*.{js,json,md}\" --ignore-path .gitignore"
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
