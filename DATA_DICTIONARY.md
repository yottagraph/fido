# Data dictionary

Prose definitions for every flavor, property, relationship, and
attribute in `schema.yaml`. Keep this file in lockstep with `schema.yaml`
— if you add a field there, document it here, and vice versa.

> Replace the `example_entity` section below with the real entities,
> fields, and relationships for this project, as named in `DESIGN.md`.

## Flavors

### `example_entity`

The primary entity this dataset describes.

- **Strong id**: `example_entity_id`
- **Mergeability**: not mergeable
- **Examples**: _list 2–3 concrete examples here_

## Properties

### `example_entity_id`

Canonical identifier for `example_entity`.

- **Type**: string
- **Encoding**: _document the exact format, e.g. "lowercase 0x-hex of
  the Ethereum address" or "UUID v4 string from the upstream API"_
- **Examples**: `replace-with-real-example`

## Relationships

_None in the template. Add a section here for each `relationships:` entry
in `schema.yaml`._

## Attributes

### `example_entity.example_attribute`

Replace with a real attribute of `example_entity`.

- **Type**: string
- **Notes**: _units, edge cases, nullability_
- **Examples**: _list 2–3 concrete examples here_
