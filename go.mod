// Replace the module path below with the project's own path before the
// first commit. The template uses a placeholder so `go build ./...`
// still works out of the box.
module github.com/example/fido-fetch

go 1.25
